

const ConfirmedBooking = (props) => {
    return (
        <h1 className="booking-detail-header">{props.success ? 'Booking confirmed' : 'Cant make reservation'}</h1>
        <section className="booking-detail">
            <
        </section>
    )
};

export default ConfirmedBooking;
